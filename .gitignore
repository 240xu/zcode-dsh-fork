node_modules
# tsc/tsdown build outputs (regenerated by `pnpm run build:lib`; the zcode
# preset tests resolve packages via tsconfig src aliases, never lib/)
lib/
*.tsbuildinfo
# local-only verification logs
*.log
